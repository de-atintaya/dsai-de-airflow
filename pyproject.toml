[project]
name = "dsai-de-airflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apache-airflow[microsoft-azure]>=3.2.2",
    "pandas>=3.0.3",
    "pendulum>=3.2.0",
]

[tool.ty.environment]
python = ".venv"
